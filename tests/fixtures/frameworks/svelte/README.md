# svelte fixture

A `.svelte` component (`export let` props). No React FC/class shape for
react-docgen-typescript to anchor on, so `parse()` returns no docs. Expected:
unsupported — `tttGet` throws "No component found".

<!-- PROPS-TABLE:START -->

<!-- PROPS-TABLE:END -->
