# astro fixture

An `.astro` component (frontmatter `interface Props`, no exported React
component). `parse()` returns no docs. Expected: unsupported — `tttGet`
throws "No component found".

<!-- PROPS-TABLE:START -->

<!-- PROPS-TABLE:END -->
