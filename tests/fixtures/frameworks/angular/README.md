# angular fixture

A plain TypeScript class, the shape of an Angular component. Not a React
FC/class component, so `parse()` returns no docs. Expected: unsupported —
`tttGet` throws "No component found".

<!-- PROPS-TABLE:START -->

<!-- PROPS-TABLE:END -->
